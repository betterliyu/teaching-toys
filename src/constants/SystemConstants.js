import keymirror from 'keymirror';

export const ActionTypes = keymirror({
  CHANGE_WINDOW_MAXIMIZED_STATUS: null,
});

export const DisplayMode = keymirror({
  Min: null,
  Max: null,
  Normal: null,
  Close: null,
});
